"use strict";

/**************************************************
Template p5 project
Pippin Barr

Here is a description of this template p5 project.
**************************************************/

// setup()
//
// Description of setup() goes here.
function setup() {
  createCanvas(400, 400);

   // Create our graphics object (like a canvas)
   drawingArea.graphics = createGraphics(drawingArea.width, drawingArea.height);
   // Set its background to black only once, so we get a drawing effect
   drawingArea.graphics.background(0)
}

// draw()
//
// Description of draw() goes here.
function draw() {

}
